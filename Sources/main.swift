import Cocoa

@main
struct ForEachApp: App {
    var body: some Scene {
        Window("ForEach 循环视图", id: "main") {
            ContentView()
        }
        .defaultSize(width: 500, height: 500)
    }
}
